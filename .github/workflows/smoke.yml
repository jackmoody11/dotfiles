name: Smoke Test
on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Print current working directory
        run: ls && cwd
      - name: Run bootstrap.sh
        run: ./bootstrap.sh
